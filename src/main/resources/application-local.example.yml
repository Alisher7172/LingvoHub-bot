# Copy this file to application-local.yml and fill in your values.
# application-local.yml is gitignored — never commit real tokens.
#
# Run with:
#   mvn spring-boot:run -Dspring-boot.run.profiles=local
#   java -jar target/lingvohub-bot-1.0.0.jar --spring.profiles.active=local

telegram:
  bot-token: "123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  bot-username: "@your_bot_username"
  admin-ids:
    - 123456789
